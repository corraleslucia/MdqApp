import 'package:flutter/material.dart';

class Category {

  String name;
  String url;
  String token;
  String method;
  IconData icono;

  Category(this.name, this.url, this.token, this.method, this.icono);
}